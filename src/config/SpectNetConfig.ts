import { ISpectNetConfig } from "./ISpectNetConfig";

/**
 * Defines the default spectrum configuration
 */
export const defaultSpectNetConfig: ISpectNetConfig = {
  model: "ZX Spectrum 48K",
  edition: "PAL",
  defaultTape: "pac-man",
  fastLoad: false
}