import { IDeviceConfiguration } from "../abstraction/IDeviceConfiguration";

/**
 * This interface represents a device with no configuration data
 */
export interface INoConfiguration extends IDeviceConfiguration {
}